# SPEC.md — ibuki-lab(息吹ラボ)

<!-- scaffold template v1.30.0 から展開(2026-09-05)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

大気中の二酸化炭素は、年に一度大きく上下する。北半球の陸上植生が夏に吸い、冬に吐くからで、
その振れ幅は北緯 70 度帯で十数 ppm、赤道付近で数 ppm、南極ではほとんど消えて位相が逆になる。
**「地球は年に一度、息をしている。北ほど深く、南では逆向きに」** —— この一文で説明が終わる。

本プロジェクトは、この年周曲線を素材に **「一本の曲線だけを見せて、その空気が地球のどの緯度で
採られたかを当てるニューラルネット」** を作り、**モデルが一年のどこを見て答えを出したか**を
曲線の上に塗って見せる。ディープラーニングの実装訓練であり、非商用・広告なしで公開する。

この素材を選んだ理由は、**正解ラベルが人の判断を経ていない**ことにある。緯度は観測所の
所在地という物理的事実で、NOAA のデータファイルのヘッダに書かれている。フリートで最も
苦労してきた「オラクルが自作でない」という条件が、素材の側で解けている
(`hoka-no-koe` の行頭 `--` と同じ性質)。

## 2. 素材

| ID | 出典 | 版 | 取得 | 権利 |
|---|---|---|---|---|
| SRC-CO2 | NOAA GML `co2_surface-flask_ccgg_text.tar.gz`(Carbon Cycle 地上フラスコ網) | 2026-07-17 | 2026-09-05 | **CC0 1.0** |
| SRC-CH4 | NOAA GML `ch4_surface-flask_ccgg_text.tar.gz` | 2026-07-17 | 2026-09-05 | **CC0 1.0** |
| SRC-JMA | 気象庁 綾里 / 南鳥島 / 与那国島 CO2 月平均値 CSV | — | 2026-09-09 | 政府標準利用規約 |

取得元 URL と SHA-256 は `data/LICENSE-DATA.md` に記録する。

### 母集団の定義

- **地点**: SRC-CO2 のうち `*_month.txt` が存在し、かつ緯度が確定するもの。
  確定の経路は二つあり、**どちらであるかを地点ごとに記録する**(§2.1)
- **標本**: `*_event.txt` の測定結果のうち、QC フラグ第 1 列(棄却)が `.` のもの
- 以下の数はすべて `pipeline/ingest.py` の実測であり、**`data/census.json` が正本**である。
  SPEC のこの節と census.json は G-14 で機械突合する

**実測 2026-09-05**(母集団 = 展開した tar の全ファイル。`python pipeline/ingest.py` で再生成)。

| 区分 | 件数 |
|---|---|
| SRC-CO2 月次ファイル | 95 |
| うち固定地点(event ヘッダに緯度あり) | 74 |
| うち船舶帯(公称緯度なし・§2.1) | 21 |
| SRC-CO2 event 標本(全 event ファイル・棄却フラグ除去後) | 217355 |
| CO2 と CH4 の両方がある地点 | 94 |

**緯度の付かなかった月次地点は 0 件**である。95 件が固定地点 74 と船舶帯 21 で尽きた。

数の母集団を取り違えないための註記:

- **event ファイルは 85 本**あり、月次ファイルのある 95 地点とは集合が違う。
  月次のみ 21 件(船舶帯。親の shipboard event に含まれる)、event のみ 11 件
  (`aoc` `hsu` `kco` `lef` `mko` `pao` `poc` `scs` `sgi` `tac` `wpc`)。
  README §7.7 が言うとおり、記録が短い・疎な地点は月次平均が作られない
- 上表の標本数は **85 本の event ファイル全体**に対するもので、95 地点に対するものではない。
  総標本 **252,686**、QC 棄却 **35,331**(14.0%)、保持 **217,355**
- SRC-CH4 の月次ファイルは **96** 件。CO2 と重なるのは 94 件で、
  CO2 のみ 1 件(`stc`)、CH4 のみ 2 件
- 確定した緯度の範囲は **−89.98 度(南極)〜 +82.45 度(Alert)**。極から極まで並ぶ

### 2.1 緯度がどこから来るか(この節がこのプロジェクトの背骨)

- **固定地点**: `*_event.txt` ヘッダの `site_latitude` / `site_longitude` / `site_elevation`。
  観測所の公称位置であり、NOAA が書いている
- **船舶帯(`poc*` / `scs*`)**: 公称緯度は `-1e+34`(欠測値)で**使えない**。
  README §7.7 が「`pocn30` は Pacific Ocean Cruise を **30N ± 2.5 度**でまとめたもの」と
  帯の定義を書いている。**帯の緯度は符号名から読まず、親の shipboard event ファイルの
  緯度分布から「船が実際に止まった目標点」を取り出して決める**(`observed_marks`)。
  符号名との一致は、独立に決まった二つの量が合ったという主張であり、検算として意味を持つ。
  対応する目標点が観測されなければ例外で止める(HC-012 / HC-075)

> **経緯(loop_000 VERIF-GAP / S2)** 最初の実装は、親の標本を
> `|緯度 − 符号名の緯度| ≤ 半値幅` で絞り込んでから、その中央値を符号名と比べていた。
> **絞り込みの条件が検算の結論を保証しており、この照合は原理的に落ちない**(HC-045)。
> 21 帯すべてで差が 0.000 度になったことで気づいた。実測すると親 POC の保持標本
> 10,997 のうち **74.0%** が格子上ちょうどで(SCS は 2,111 中 55.8%)、中央値は
> 必ず公称値に一致する。緯度分布のみから目標点を導く方式に置き換えた。
>
> **置き換えが恒等式でないことの証拠**(実測 2026-09-05): POC の観測目標点は **17 点**、
> 符号名から導かれる帯は **14 点**で、集合が一致しない。差は **35N / 40N / 45N** ——
> 船は止まっているが月次ファイルが作られていない緯度である(README §7.7 の
> 「記録が疎な系統は月次平均を作らない」に符合する)。SCS は 7 点で一致。

### 2.2 月次ファイルは生の平均ではない(L0 実測 2026-09-05)

README §7.7 の記述:

> Monthly means are produced for each site by first averaging all retained measurement results
> in the event file with a unique sample date and time. Values are then extracted at weekly
> intervals from a smooth curve (Thoning et al., 1989) fitted to the averaged data and these
> weekly values are averaged for each month to give the monthly means recorded in the files.

つまり `*_month.txt` の値は **多項式 + 調和関数の当てはめを経た平滑値**である。
これを模型の入力にすると、模型は大気ではなく **NOAA の当てはめ基底**を学ぶ側面を持つ。
とくに「振幅と位相の 2 特徴」という対照は、平滑済みの系列に対しては原理的に強くなる。

したがって **学習と評価の入力は `*_event.txt`(保持された生の測定結果)から作る**(G-06)。
`*_month.txt` は画面①の表示と、平滑の影響を測る対照系統にのみ使い、
画面には「平滑済み」と明示する。

### 2.3 年周曲線をどう組むか(L1 で確定)

**単位は「地点年」**である。ある地点のある一年ぶんの標本から、一本の年周曲線を作る。

1. **重複時刻の平均**: 同一時刻の標本(フラスコ対)を先に平均する。
   NOAA 自身の月次平均も同じ順序を踏んでいる(README §7.7)。
   実測 2026-09-06: 標本の 49.0% が同一時刻の重複で、対の値の開きは中央値 0.077 ppm
2. **24 の半月ビン**に落とし、ビン内平均を取る。ビン境界は年内の小数位置を 24 等分する
3. **全ビンが埋まっている地点年だけを採る。補間しない。**
   これは §2.2 と同じ理由による —— 欠測を補間すれば、それは平滑であり、
   模型は大気でなく補間器を学ぶ。埋まらない地点年は落とし、落とした数を出す(G-16)
4. **直線を引いて差し引く**(地点年ごと)。ビン中心時刻に対する最小二乗直線を引き、残差を取る。
   これで **絶対濃度(半球間の勾配 = 緯度の手がかり)と年内の増加が同時に消える**(G-02)
5. 残差は **ppm のまま**にする。振幅こそが信号なので、振幅を正規化して捨てない

**ビン数の選択は実測による**(2026-09-06、標本 40 件以上の 1,926 地点年):
全ビンが埋まる割合は 12 ビンで 77.7%、**24 ビンで 41.8%**、52 ビンで 3.8%。
24 を採ったのは、補間なしで固定地点から **805 地点年**が確保でき、かつ 12 ビンより細かいためである
(24 ビンが埋まる 806 地点年のうち 1 件は船舶帯 `scsn09` で、下の理由により学習からは外れる)。

**この規則は船舶帯を全滅させる。** 周航は各緯度を年に数回しか通らないので、
24 ビンを一年で埋められない。したがって **船舶帯は学習に使わず、複数年をまとめた
気候値曲線として第二の外部ホールドアウトに回す**(F-02 の緯度梯子はこの気候値で描く)。

## 3. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 画面①「呼吸する地球」: 緯度 × 半月の年周偏差をヒートマップにし、年内スライダで印を動かす。**絶対の目盛りと行ごとの正規化を切り替えられる**(前者は量、後者は位相の反転を見せる) | must |
| F-02 | 画面①に**太平洋の緯度梯子**(船舶帯 35S–30N)を重ね、振幅の消失と位相反転を一枚で見せる | must |
| F-03 | 画面②「積み上がる息」: 1970 年代からのキーリング型の上昇と、トレンドを外した一呼吸の切替 | must |
| F-04 | 画面③「当ててみる」: 地点を選ぶ / 曲線を描くと、ブラウザ内の模型が緯度帯を地球儀に描く | must |
| F-05 | 画面③で**顕著度を 1 年 52 週の曲線の上に塗る**。どの季節が答えを決めたかを絵で見せる | must |
| F-06 | 画面④「解剖台」: ベースライン対決・陰性対照・地点別誤差地図・二実装照合の残差 | must |
| F-07 | 画面④に**落ちた目玉の記録**を掲示する。通らなかったゲートを消さない | must |
| F-08 | 気象庁 3 地点(NOAA が一度も見ていない網)での外部検証を独立の節として出す | should |
| F-09 | CH4 を第 2 チャンネルに足した系統との比較を出す | should |
| F-10 | 解説「歩き方」「設計図」。フリート共通フッタ規約 5 項目 | must |

## 4. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 課金経路ゼロ。cron・サーバ関数・外部 API を使わない静的配信 | 出荷物の実測 |
| N-02 | 模型は 1 MB 未満。ブラウザ推論はランタイム依存ゼロ(重み JSON + 自前 TS 前向き) | 出荷物のバイト数 |
| N-03 | 主要 3 画面幅(360 / 768 / 1280 px)で横溢れ 0・縦 16,000px 以下 | 実ブラウザ検品(HC-078) |
| N-04 | 学習は手元(CPU)で完結する。GPU を要求しない | 実測時間を記録 |
| N-05 | フリート共通フッタ規約 5 項目を満たす | 目視 + 検査 |

## 5. 品質ゲート

| ID | 内容 | 判定 |
|---|---|---|
| G-01 | **二実装照合**: JAX の推論と TS 前向きが全標本で `|Δ| < 1e-6`。結論だけでなく**中間層の活性**も比較する(HC-065)。経路をずらした実装を照合が落とすことを陽性対照で示す | **通過**(L3, T-035〜T-039・§7.12。float64 どうし 1e-9 / 出荷経路は無次元化 1e-6) |
| G-02 | **循環の禁止**: 正解の緯度は NOAA のヘッダと README の帯定義からのみ導く。**入力側から緯度の手がかりを機械的に除去する** —— 地点年ごとに平均と線形トレンドを除去し、地点コード・年・経度・標高を入力に含めない | 実装済(L0/L1, T-002/T-012/T-013) |
| G-03 | **陰性対照**: 地点に緯度ラベルを入れ替えて割り当てると、誤差が「常に全体平均緯度を答える」水準を**下回らない**(片側) | **通過**(L1, T-017。入替 36.10 対 自明 31.21) |
| G-04 | **地点ばらし split**: 同一地点の標本が train/test に跨らない。近接地点(大円距離 500 km 未満)は同一グループに寄せる | 実装済(L1, T-015/T-016。74 地点 → 64 群) |
| G-05 | **移動プラットフォームの分離**: 公称緯度が欠測値(`-1e+34`)の系統を固定地点として扱わない。船舶帯の緯度は**符号名を見ずに**緯度分布から導いた目標点で決め、符号名と対応することを検算する。対応しなければ例外で止める。照合が恒等式でないことを差集合で示す | 実装済(L0, T-003/T-004) |
| G-06 | **平滑済みデータで学習しない**: 学習・評価の入力は `*_event.txt` 由来であることをコードで固定する。`*_month.txt` を使う系統は「平滑済み」と画面に明示する | **通過**(L0/L4, T-005/T-006/B-07) |
| G-07 | 本文・ソースの字種検査(`harness/text_hygiene.py`) | 実装済(L0, T-007) |
| G-08 | **目玉①(事前登録)**: 年周曲線だけで、学習に一度も出ていない地点の緯度を当てられる。判定は §6 P-01 の閾値による | **通過**(L2, T-033・§7.9。12.40 対 閾値 15.61) |
| G-09 | **目玉②(事前登録)**: ニューラルネットが「年周振幅と位相の 2 特徴」の系のうち**強いほう**を上回る。相手は L1 実測で **2 特徴 kNN・地点 MAE 10.95 度**(§7.3) | **不通過**(L2, T-033・§7.9。12.40 対 10.95。P-02 が当たった) / **L8: nested で選び直すと位相ありも 11.55 度で、相手の 10.95 度を下回らない**(§7.18) |
| G-10 | **目玉③(事前登録)**: CH4 を第 2 チャンネルに足すと緯度誤差が下がる。同一母集団(739 地点年)で比べる | **通過**(L2, T-031/T-033・§7.9。11.69 → 10.76。P-03 が外れた。§7.16 で切り分け済み: 入れ替えた CH4 は 2.48 度悪化するので、効いたのは情報であって「チャンネルが増えたこと」ではない) / L8: CH4 の振幅を揃えても効き目の半分が残り、位相差を壊すとほぼ消えた。要素は特定していない(§7.18) |
| G-11 | **外部ホールドアウト**: 気象庁 3 地点(独立の観測網)で緯度を当てる。NOAA の学習・検証・調整のどの段階でも気象庁データを見ないことをコードで固定する | **通過**(L5, T-046〜T-051・§7.14。学習経路が jma を読まないことを走査で確認) |
| G-12 | **目玉④(事前登録・DL 抜き)**: 高緯度地点の年周振幅は記録期間中に増大している。純統計で測り、置換検定の対照を置く | **通過**(L1, T-018・§7.6。標本数の交絡を調整しても残る) |
| G-13 | 図の要素が `viewBox` に収まる(`getBBox()` で実測 — HC-159)。`defs` 配下は除く(HC-194) | **通過**(L4, B-02/B-02c) |
| G-14 | **文書中の数値の突合**: SPEC §2 の素材表が `data/census.json` と一致する。ずれた数を検出できることを陽性対照で示す(HC-152) | 実装済(L0, T-001/T-008〜T-011) |
| G-15 | 図・可視化を触ったループは実ブラウザ検品を完了条件に含める(HC-041 / HC-138) | **通過**(L4, `scripts/browser_check.mjs` 12 件・陽性対照つき) |
| G-16 | **被覆の明示**: 欠測を埋めた区間と実測区間を区別し、充填率を**分子と分母**で表示する。埋めた値を実測と見せない | **通過**(L1/L4, T-014/B-08。補間 0・採用率 805/1818 を画面に分子と分母で出す) |

## 6. 事前登録した予測(L0 時点で書き、後で測る)

外れた予測は消さずに実測と併記して残す(HC-077)。
**P-01 の閾値は §7.2 の決め方で L1 に確定した(15.61 度)。以後動かさない。**

| ID | 予測 | 根拠 | 判定 |
|---|---|---|---|
| P-01 | 年周曲線だけで未知地点の緯度を当てられる(閾値は L1 で確定・以後不変) | 年周振幅の緯度依存は観測事実として広く知られている | **当たった**(L2・§7.9。12.40 ≤ 15.61) |
| P-02 | ニューラルネットは「振幅と位相の 2 特徴の回帰」を**上回らない** | 年周曲線の情報の大半が振幅と位相に集約されているため | **当たった**(L2・§7.9。12.40 対 10.95) |
| P-03 | CH4 を足しても緯度誤差は**下がらない** | CH4 の季節性は発生源起源で、緯度への写像が CO2 ほど単調でないため | **外れた**(L2・§7.9/§7.11。11.69 → 10.76) |
| P-04 | 誤差は南半球と熱帯で大きい(振幅が小さく手がかりが乏しい) | 振幅が信号なら振幅が小さい側で不利になる | 未測定(L2) |
| P-05 | `*_month.txt`(平滑済み)で学習すると、2 特徴回帰との差は event よりさらに縮む | 平滑が調和関数へ寄せるため | 未測定(L2) |
| P-06 | 高緯度の年周振幅は記録期間中に増大している | 文献の主張。自前で測り直す | **成立**(L1・§7.6) |
| P-07 | **位相を見える要約に替えると、2 特徴 kNN の 10.95 度を下回る** | L6 で分かった機構(§7.15)。相手と同じ手がかりを持たせれば勝てるはず | **数の上では成立**(L7・§7.16。10.71 対 10.95。ただし種の幅 3.38 が差の 14 倍で「勝った」とは言わない) → **nested では不成立**(L8・§7.18。11.55 対 10.95) |
| P-08 | **学習率の選定を nested にすると、位相ありの模型の種の幅が縮む**(L7 の 3.38 未満) | L7 の幅は lr=0.03 の高さで説明がつく(§7.16)。fold ごとに選べば全 fold が高すぎる学習率に乗らない | **成立**(L8・§7.18。幅 3.38 → 1.26) |
| P-09 | **止め時の分割と学習率を選ぶ分割を分けると、選んだ学習率の試験誤差が良くなる** | §7.16 の診断: 止め時に使った分割は、学習率の比較に対して偏った推定量になる | **成立(平均で)**(L8・§7.18。11.55 対 11.88。種 2 だけは逆) |
| P-10 | **CH4 の効き目は振幅が運んでいる**(CO2 との位相差ではない) | 模型は緯度でなく年周振幅を読む(§7.14) | **外れた**(L8・§7.18。振幅を揃えて r=0.50、位相差を壊して r=0.08 と予測の逆向き。要素は特定しない) |
| P-11 | **nested にしても、位相ありは位相なしを下回る**(位相の効果は選定規則の産物ではない) | L7 の改善 1.69 度は、位相なしの系の種の幅 1.23 より大きい | **成立**(L8・§7.18。11.55 対 12.52。ただし差 0.97 は位相なしの種の幅 2.21 より小さい) |

**P-02 と P-03 は「落ちる」側に賭けた予測である。** 落ちた場合は画面④に
「ディープラーニングは 2 特徴の回帰に勝てなかった」をそのまま出す。これは失敗ではなく
測定結果であり、本プロジェクトの出荷物の一部とする。

## 7. ベースライン測定(L1)

### 7.1 何と何を比べるか

すべて**地点群ばらしの leave-one-group-out 交差検証**で測る(G-04)。
群は固定地点を大円距離 500 km 未満で併合して作る(実測 2026-09-06: 74 地点 → 64 群)。

| 系 | 中身 | 役割 |
|---|---|---|
| 平均予測器 | 学習側の緯度の平均を常に答える | **自明な下限**。P-01 の閾値はここから決める |
| 中央値予測器 | 学習側の緯度の中央値を常に答える | MAE に対してはこちらが自明な最良 |
| 2 特徴・線形 | 第一調和の係数 `(a1, b1)` に対する線形回帰 | 「振幅と位相だけ」の最小構成 |
| 2 特徴・kNN | 同じ 2 特徴に対する k 近傍(k は学習側だけで決める) | 非線形を許した 2 特徴の上限 |
| 24 次元・kNN | 曲線 24 点そのものに対する k 近傍 | 特徴を作らずに全形を使った場合 |

**目玉②(G-09)が上回るべき相手は、この表の 2 特徴系のうち強いほう**である。
弱いほうを選んで勝つのは対照ではない(HC-064)。

### 7.2 P-01 の閾値(L1 で確定・以後動かさない)

**中央値予測器の MAE の半分**を閾値とする。この決め方は模型の成績を見る前に定めており、
以後動かさない。実測値は §7.3 に置く。

### 7.3 測定結果(実測 2026-09-06)

母集団は **805 地点年 / 65 地点 / 57 群**。正本は `data/baseline.json`
(`python -m pipeline.baseline` で再生成)。採否の内訳は §7.4。

指標は **地点あたりの MAE**(地点ごとに誤差を平均してから、地点間で平均)。
地点ごとの年数が 1〜41 年と桁で違うので、例あたりだけを見るとよく採れた地点が結論を握る。

| 系 | MAE(地点) | MAE(例) | 最悪の地点 |
|---|---|---|---|
| 平均予測器 | 30.91 | 33.25 | 119.06 |
| 中央値予測器 | 31.21 | 34.05 | 126.27 |
| 2 特徴・線形 | 16.21 | 15.20 | 107.01 |
| **2 特徴・kNN** | **10.95** | 11.69 | 51.80 |
| 24 次元・kNN | 12.67 | 13.13 | 61.29 |

**陰性対照(G-03)通過**: 地点単位で緯度を入れ替えると MAE は **36.10 度**(20 回平均、
最小 29.96)。自明な中央値予測器の 31.21 すら下回らない。入れ替えたほうが悪くなるのは、
曲線が実際に緯度を指しているからである。

### 7.4 この測定から先に言えること

- **信号はある。** 2 特徴 kNN の 10.95 度は、自明な予測器の 31.21 度の三分の一である
- **24 次元をそのまま使うと悪くなる**(12.67 対 10.95)。曲線の残り 22 次元は、
  少なくとも kNN にとっては雑音として働いている。**これは P-02 に有利な材料**である
- **P-01 の閾値 15.61 度(中央値予測器の半分)は、2 特徴 kNN が既に満たしている。**
  したがって G-08 は低い障壁であり、**判定の主戦場は G-09(模型が 10.95 を切るか)**である。
  閾値の決め方は模型を見る前に定めてあるので動かさないが、低かったことは明記する
- **最悪の地点は 51.80 度ずれる。** 平均振幅 38.49 ppm の `hun`(ハンガリー・北緯 46.96)が
  外れ値で、他の同緯度帯(`cba` 17.66)の倍以上ある。内陸の地点は局所の発生源を拾う

### 7.5 採否の内訳(G-16)

| 区分 | 地点年 |
|---|---|
| 標本 40 件以上の地点年 | 1926 |
| うち固定地点 | 1818 |
| **採用(24 ビンすべてが実測)** | **805** |
| 欠けビンで落とした | 1013 |
| 船舶帯として外した | 108 |

**採用率は 805 / 1818 = 44.3%** である。落とした 1,013 地点年は補間すれば使えるが、
補間は平滑であり §2.2 の理由で採らない。**落とした数を隠さずに出す。**

### 7.6 年周振幅の経年変化(G-12 / P-06 — 実測 2026-09-06)

採用年が 20 年以上ある **15 地点**を、年と振幅の対応を入れ替える置換検定(5,000 回)で測る。
振幅は第一調和の大きさではなく**実測の山と谷の差**で取る。正本は `data/amplitude.json`。

| 緯度帯 | 地点 | 傾きが正 | 正かつ p<0.05 | 標本数調整後も正 |
|---|---|---|---|---|
| 北緯 45 度以北 | 6 | 5 | 4 | **6** |
| 北半球 0–45 度 | 5 | 5 | 1 | 4 |
| 南半球 | 4 | 1 | 0 | 1 |

**交絡を先に測った(HC-025)。** 標本数と振幅の相関は 15 地点中 12 地点で負
(中央値 −0.165)—— 標本が少ないほど山谷が雑音で開く。しかも `alt` / `zep` / `brw` では
標本数が年とともに**減って**おり(r = −0.247 / −0.565 / −0.521)、
**交絡は「振幅が増えた」という主張と同じ向きに効く**。

そこで `振幅 ~ 年 + 標本数` を解き直した。**主張は生き残った**:

| 地点 | 緯度 | 年数 | 平均振幅 | 素の傾き | p | 標本数調整後 |
|---|---|---|---|---|---|---|
| alt | 82.45 | 29 | 18.61 | 0.0518 | 0.0378 | 0.0447 |
| zep | 78.91 | 21 | 19.53 | 0.1515 | 0.0002 | 0.1528 |
| sum | 72.60 | 21 | 16.11 | 0.0795 | 0.0380 | 0.0628 |
| brw | 71.32 | 37 | 20.03 | 0.1109 | 0.0002 | 0.1097 |
| spo | −89.98 | 27 | 1.26 | −0.0164 | 0.0062 | −0.0206 |

**G-12 は通過。P-06 は成立**とする。ただし言えるのはここまでである。

- 言える: **北緯 45 度以北の 6 地点すべてで、標本数を調整しても振幅の傾きは正**。
  うち 4 地点は素の置換検定でも p < 0.05
- 言える: **南極では逆に有意に減っている**(p = 0.0062)。ただし振幅そのものが 1.26 ppm と
  小さく、絶対量は年 0.02 ppm である
- **言えない**: 増えた理由。北方林の活動が強まったからだとは、このデータからは出ない(§8)
- **注意**: 15 地点を α = 0.05 で検定しているので、偶然の有意は 0.75 件見込まれる。
  観測された有意は 6 件で、**符号の揃い方**(北 10/11 が正)のほうが証拠として強い

### 7.7 学習側の構成(L2)

- **JAX 0.11.1(素)+ optax 0.2.8。Flax は使わない。** Windows の MAX_PATH 制限で
  orbax-checkpoint が展開できず `pip install flax` が止まるため(loop_002 TOOL-ENV)。
  前向きを自前で書くほうが G-01 の TS 実装が直訳になるので、制約を利点として受ける
- 依存は `requirements.txt` に版を固定して書く。**このシェルは VIRTUAL_ENV を
  別プロジェクトから引き継ぐ**(HC-171)ので、コマンド例は `.venv/Scripts/python.exe`
  の絶対名で書き、裸の `python` を残さない
- 模型は **24 点 1 チャンネル(CH4 を足す系は 2 チャンネル)の 1D CNN**。
  独立標本は 57 群しかないので、規模はそれに見合う小ささにする
- 評価は L1 と**同じ 805 地点年 / 57 群の leave-one-group-out**、指標も**地点あたり MAE**。
  相手と同じ土俵に載せる。学習の乱数種は固定し、種を変えた再現も測る

### 7.8 模型の測定結果(L2 — 実測 2026-09-08)

正本は `data/model.json`(`.venv/Scripts/python.exe -m pipeline.experiment` で再生成)。
土俵は L1 と同じ **地点群ばらし leave-one-group-out / 地点あたり MAE**。
種を 0/1/2 の 3 通りで回し、**平均を成績とし、幅も併記する**。

| 系 | MAE(地点) | 地点年 | 重み |
|---|---|---|---|
| **1D CNN(CO2 のみ)** | **12.40** | 805 | 521 |
| 1D CNN(CO2 のみ・対照母集団) | 11.69 | 739 | 521 |
| 1D CNN(CO2 + CH4) | 10.76 | 739 | 561 |

種ごとの値: 主系統 11.98 / 13.22 / 12.01(幅 **1.23**)、対照母集団の CO2 のみ
10.53 / 13.17 / 11.38(幅 **2.63**)、CO2+CH4 10.91 / 10.51 / 10.85(幅 **0.40**)。

### 7.9 目玉の判定

| ゲート | 内容 | 相手 | 模型 | 判定 |
|---|---|---|---|---|
| G-08 | 未知の地点の緯度を当てられるか | 閾値 15.61 | 12.40 | **通過** |
| G-09 | 2 特徴の系のうち強いほうを上回るか | 10.95 | 12.40 | **不通過** |
| G-10 | CH4 を第 2 チャンネルに足すと下がるか | 11.69 | 10.76 | **通過** |

**P-02(模型は 2 特徴の回帰を上回らない)は当たった。P-03(CH4 は効かない)は外れた。**
どちらも「落ちる側」に賭けた事前登録である。§6 の表と併せて残す。

### 7.10 この判定を疑うために測ったこと

**(a) 打ち切りが負けを作っていないか。** 早期打ち切りは模型の成績を下振れさせるので、
「模型が負けた」という結論には都合よく働く。予算を 3 倍(600 → 1800 エポック)にして
測り直すと **11.98 → 11.69(−0.29 度)**。事前登録した閾値 **0.35 度**
(相手との差 1.45 度の 4 分の 1 弱)を下回るので、**打ち切りは結論を作っていない**。

> 予算に張り付いた fold は主系統で 171 走中 **1 件**ある。件数を 0 に追い込むのは
> やめた —— 種が変われば内側分割も変わるので終わりが無く、**問うべきは
> 「打ち切りが結論を動かすか」であって「張り付きが 0 件か」ではない**(HC-218)。

**(b) 学習率を選んだせいで負けていないか。** 掃引(種 0)は次のとおり。
**選定は内側検証だけで行い**、試験側の数字は選定に使わずに併記する。

| lr | 内側検証 | 試験 | 予算張り付き | 選定対象 |
|---|---|---|---|---|
| 0.003 | 9.52 | 11.99 | 1 件 | 失格 |
| 0.01 | 9.09 | 12.08 | 0 | ○ |
| **0.03** | **8.51** | 11.98 | 0 | **採用** |

**三つとも相手の 10.95 に届かない。** したがって G-09 の判定は学習率の選び方に依存しない。
なお**内側検証で選ぶと、試験側だけを見て選ぶ場合と違う候補になる**(試験だけなら 0.03 と
0.003 が拮抗する)。試験を見て選べば数字は良くなるが、それはもう成績ではない。

**(c) CH4 の効果は「同じ土俵」で見たか。** 対照母集団(739 地点年 / 56 群)の
ベースラインも同じ手順で測った(正本 `data/baseline_paired.json`)。

| 系 | MAE(地点) |
|---|---|
| 中央値予測器 | 29.81 |
| 2 特徴・線形 | 16.36 |
| **2 特徴・kNN** | **10.86** |
| 24 次元・kNN | 12.88 |
| ラベル入替対照 | 34.10 |

**CO2+CH4 の 10.76 は、同じ母集団の 2 特徴 kNN(10.86)と 0.10 度しか違わない。**
種による幅 0.40 度より小さいので、**勝ったとは言わない**。
主母集団のベースライン(10.95)と並べれば勝ったように見えるが、それは違う土俵である。

### 7.11 G-10 が通ったことの読み方(慎重に)

CH4 を足すと 11.69 → 10.76 と 0.93 度下がった。ただし次の三点を併せて読む。

- **平均の差は外れ値に押し上げられている。** CO2 のみの種 1 が 13.17 と外れており、
  中央値で比べると 11.38 → 10.85 で差は **0.53 度**にとどまる
- **幅の縮み方のほうが著しい。** 種による幅は 2.63 → **0.40** と 6 分の 1 になった。
  これは「CH4 が緯度を教えた」というより「第 2 チャンネルが学習を安定させた」に見える
- **本データではこの二つを分けられない。** 分けるには、CH4 を無情報な系列
  (順序を崩した CH4、別地点の CH4 等)に差し替えた対照が要る。**L7 の課題とする**

したがって画面には「**CH4 を足すと誤差が下がった。ただし緯度の情報が増えたのか、
学習が安定しただけかは切り分けていない**」と書く。

### 7.12 出荷する模型と、二実装照合(L3 — 実測 2026-09-08)

**出荷する模型には保留した試験が無い。** 全 805 地点年で学習した一本だからである。
止め時だけは L2 と同じ作法で決めた —— 群単位で切り出した内側検証(226 例)が最良の点、
**249 エポック**。

**この模型自身の成績は測れない。** 画面に出す「当たり具合」は L2 の
leave-one-group-out 実測 **12.40 度**であり、これは同じ手続きで学習した模型が
**未知の地点**でどれだけ外すかの見込みである。重みの `metadata` にも同じ断りを書き、
T-040 が両方に書かれていることを確かめる。

出荷物は **重み 14.6 KB**(521 パラメータ)。N-02 の上限 1 MB を大きく下回る。
ブラウザ側の前向きは外部ライブラリを読まない(T-041 が import を走査して確かめる)。

#### 閾値は「一致しうる量」ごとに分けて置く

学習側は float32、ブラウザ側は JS の数値すなわち float64 である。
**±90 度の量に絶対 1e-6 を要求するのは原理的に達成できない**(HC-073)。
そこで三つに分けた。

| 比べるもの | 閾値 | 実測(2026-09-08) |
|---|---|---|
| TS 対 **float64 で通した基準**(出力・中間層とも) | 1e-9 | 通過 |
| TS 対 出荷経路(float32)の出力 | 無次元化して 1e-6(= 度で 9e-5) | 最大 3.80e-5 度 |
| TS 対 出荷経路(float32)の中間層 | 1e-6(活性は [-1, 1]) | 通過 |

**float64 どうしが 1e-9 で合うことが「移植の式が同じ」の証拠**である。
float32 との差(度で最大 3.80e-5、無次元化して 4.22e-7)は丸めの床であって
移植の誤りではない。差が出力の大きさにほぼ比例しないこと
(|出力|>40 で平均 8.70e-6、|出力|<10 で 6.75e-6)も、構造的なずれでないことと整合する。

> **経緯(loop_003 VERIF-FALSE / S3)** 最初は度の絶対値 1e-6 を全部に置いた。
> 中間層は通り、出力だけが 3.80e-5 で落ちた。**同じ 1e-6 を、単位も大きさも違う量へ
> 機械的に置いていた**のが誤りである。

#### 経路まで比べる(G-01)

出力が合っても、同じ理由で合ったとは限らない(HC-065)。中間層 `h1` `h2` `pooled` `d1` を
緯度の広がりを保って抜いた 24 例で照合し、あわせて**わざと壊した実装を照合が落とすこと**を
確かめる。

| 変異体 | 何を取り違えるか | 照合の反応 |
|---|---|---|
| `max_without_abs` | `max(|h2|)` を `max(h2)` に | 経路(pooled)が食い違う。**出力だけを見る照合は一部の例を見逃す** |
| `zero_padding` | 端の巡回を 0 埋めに | 全 805 例の 9 割超で出力が食い違う。一年が環である前提が効いている証拠 |

変異体は `src/model/variants.ts` に隔離し、**出荷経路から読まれていないこと**を
走査で確かめる(T-041)。

### 7.13 画面①で決めたこと(L4 — 実測 2026-09-08)

**縦は地点でなく緯度帯にする。** 地点は北緯 40〜60 度に偏っており(11 地点 + 5 地点)、
そのまま並べると南半球が数行に潰れる。等幅 10 度の帯に均し、**観測地点の無い帯は
空のまま残す** —— 詰めると緯度の目盛りが嘘になる。

**色の目盛りは二通り用意した。** これは装飾ではなく、片方だけでは嘘になるからである。

| 表示 | 見せるもの | 実測 |
|---|---|---|
| 絶対(±8 ppm で頭打ち) | **量**。北が深く南が浅いこと | 帯行の \|値\| は北緯 40〜90 度で 8.8〜9.9 ppm、南緯 10 度以南で 0.50〜1.17 ppm |
| 行ごとの正規化 | **形**。位相が南で反転すること | 正規化すると南緯 80 度以南の行の色数が 11 → 24 種になる(B-11) |

> **経緯(loop_004 GEN-LAYOUT / S3)** 絶対の目盛りだけで出したところ、南半球の行が
> ほぼ白になり、**「呼吸が浅い」ではなく「データが無い」と読める**図になっていた。
> 検品 11 件は全部緑で、スクリーンショットを見るまで気づかなかった。
> 尺度を南に合わせれば北が飽和し、量という肝心の主張が消える —— どちらか一方に
> 寄せるのでなく、**用途を書いて両方出す**ことにした。

**太平洋の緯度梯子(F-02)は、地点差を消した対照である。** 同じ船・同じ海・同じ研究室で
緯度だけを 5 度おきに振った 14 帯なので、内陸か島か・近くに都市があるかといった
地点ごとの事情が揃っている。振幅は **30°N で 9.6 → 赤道で 2.4 → 35°S で 3.6 ppm**。

船舶帯は一年で 24 ビンを埋められないので**複数年をまとめた気候値**である(§2.3)。
地点の曲線とは作り方が違うので、同じ図に重ねず別の図にした。

### 7.14 外部ホールドアウトでの検証(L5 — 実測 2026-09-09)

気象庁の綾里・南鳥島・与那国島に、L3 の出荷模型をそのまま当てた。
**学習にも、閾値の決定にも、学習率の選定にも一度も使っていない。**
それを保証するのは宣言ではなく走査である —— 学習経路のモジュールが `pipeline/jma.py` を
読んでいないことを T-048 が機械的に確かめる。

正本は `data/external.json`(`python -m pipeline.external` で再生成)。

#### そのままでは何を測ったか言えない

気象庁が配るのは**月平均 12 点**で、模型が学習したのは **event 由来の半月 24 点**である。
外れたとして、**網が違うからか、資料形が違うからか。** 分けるために三つを並べた。

| 系 | MAE(度) |
|---|---|
| NOAA・event(同じ模型の基準) | 8.34 |
| NOAA・月次(資料形の対照) | 12.39 |
| 気象庁 3 地点 | 11.42 |

上の二つは**学習に使った例**なので絶対値は楽観的である。読むのは差のほう。

- **資料形の効果 +4.05 度**(NOAA の月次 − NOAA の event。網が同じで資料形だけ違う)
- **網の効果 −0.97 度**(気象庁 − NOAA の月次。資料形が同じで網だけ違う)

**劣化のほとんどは資料形の側にある。** 別の機関が別の較正で運用する網へ移ること自体は
ほとんど効いていない。気象庁での 11.42 度は、未知の地点に対する見込み
(L2 の leave-one-group-out 実測 12.40 度)と同程度である。

なお資料形の効果 +4.05 度は、L0 で「平滑済みの月次では学習しない」と決めた判断
(§2.2 / G-06)を後から裏づける数でもある。

#### 同じ緯度の二地点が 14.6 度割れた

| 地点 | 本当の緯度 | 模型の答え | 誤差 |
|---|---|---|---|
| 綾里 | 39.03 | 56.4 ± 6.7 | 17.3 |
| 与那国島 | 24.47 | 30.5 ± 6.9 | 7.9 |
| 南鳥島 | 24.28 | 15.9 ± 6.2 | 9.1 |

**南鳥島と与那国島は緯度が 0.19 度しか違わない**(24.28 と 24.47)。経度は 31 度離れており、
片方は太平洋の孤島、もう片方は大陸のすぐ手前にある。模型はこの二つに **14.6 度**違う
答えを出した。

したがって言えるのは次までである。

- 言える: **模型が読んでいるのは緯度そのものではなく年周振幅**であり、
  振幅は大陸への近さで決まる。学習に使った地点では緯度と大陸への近さが絡んでいた
  (北の地点ほど陸に近い)ので、模型はその絡んだ手がかりを覚えた
- 言える: 綾里(内陸寄りの日本)に北緯 56 度と答えたのも同じ理由で説明がつく
- **言えない**: どちらの手がかりがどれだけ効いたかの内訳。分けるには経度や海陸の
  距離を振った対照が要る(**L7 の課題**)
- **この割れ方は、同緯度の二地点を並べるまで見えなかった。**
  NOAA の網の中では緯度と大陸への近さがほぼ同時に動くので、内訳を問う機会が無かった

### 7.15 模型は位相を見ていない —— G-09 の負けの機構(L6 — 実測 2026-09-09)

画面③の検査で分かった。**年周曲線の位相を半年ずらしても、模型の答えは 1.5e-14 度しか
動かない。** 実ブラウザでも確かめてある(B-17: 振幅を 2.4 倍にすると答えの印が 39.5px
動くのに対し、位相を半年回すと 0.00px)。

これは学習の失敗ではなく、**構造から来る性質**である。

1. 巡回畳み込みは、入力のずらしに対して**同変**である(出力も同じだけずれる)
2. そのあとの要約は「時間方向の平均」と「時間方向の絶対値の最大」の連結で、
   どちらもずらしに対して**不変**である
3. したがって前向き全体がずれを見ない

`pipeline/model.py` の該当箇所には「どちらも巡回のずらしに対して不変である」と
**自分で書いてあった**。頑健さのつもりで置いた性質が、
**半球を分ける唯一の手がかりを捨てていた。**

#### G-09 の読み方を書き直す

| | 持っている手がかり |
|---|---|
| 相手(2 特徴 kNN) | 第一調和の係数 a₁ と b₁ = **振幅と位相** |
| この模型 | ずらしに不変な要約 = **振幅の大きさだけ** |

**測定は正しい。12.40 対 10.95 は動かない。** 動くのは読み方である。

- **言えない**: 「ディープラーニングはこの問題で 2 特徴の回帰に勝てない」
- **言える**: 「**位相を捨てた**この模型は、位相を持つ相手に勝てない」

L5 で見つけた性質(同じ緯度の南鳥島と与那国島で答えが 14.6 度割れる)も、これで説明が
つく。位相が使えないので、模型は振幅の大きさだけで緯度を当てるほかない ——
そして振幅は大陸への近さで決まる。南半球の地点が「振幅が小さいから低緯度」と
答えられるのも同じ理由である。

#### 事前登録(L7 で測る)

§6 に **P-07** を足した。「位相を見える要約に替えると、2 特徴 kNN の 10.95 度を下回る」。
**外れる側にも賭けていない予測**である —— 位相を足せば相手と同じ手がかりを持つので、
勝てなければ「特徴の作り方が悪い」以外の理由を探すことになる。

具体的には、ずらしに不変な要約に**位相を保つ量**を足す。候補は
(a) 第一調和の係数を模型の中で計算して連結する、(b) 要約を取らずに時間方向を平らに並べる。
どちらも「模型に位相を渡す」だけで、正解を渡すわけではない(G-02 は保たれる)。

### 7.16 位相を足して測り直した(L7 — 実測 2026-09-10)

§7.15 で見つけた機構に対する答え合わせ。**要約の取り方だけを変えた ablation** である ——
層の数も幅も学習率の掃引の仕方も評価の土俵も同じで、`model.POOL_MODE` の一つの切替しか
動かさない。増えた重みは 128 個(521 → 649)で、これは要約の増分ちょうどである(T-057)。

位相ありの要約は、学習した各チャンネルの**第一調和の係数** (a_c, b_c) を連結する。
生の入力の第一調和を渡せば相手の特徴そのものになるので、**学習後のチャンネルに対して**取る。
渡すのは「位相を見る道具」であって答えではない(G-02 は保たれる)。

#### P-07 の判定 —— 数の上では成立、しかし勝ったとは言わない

| 系 | MAE(地点) | 種ごと | 幅 |
|---|---|---|---|
| 1D CNN(位相なし・L2) | 12.40 | 11.98 / 13.22 / 12.01 | 1.23 |
| **1D CNN(位相あり・L7)** | **10.71** | 12.47 / 10.57 / 9.09 | **3.38** |
| 相手(2 特徴 kNN) | 10.95 | — | — |

**位相を足すと平均が 1.69 度縮んだ。** これは位相なしの系の種の幅(1.23)より大きく、
意味のある改善である。§7.15 の診断は当たっていた。

**相手の 10.95 も 0.24 度下回った。事前登録した P-07 は、数の上では成立する。**

ただし**種による幅が 3.38 度あり、相手との差の 14 倍**である。
**「2 特徴の回帰に勝った」とは言わない。** 言えるのは次まで。

- 言える: **位相を捨てていたことが負けの原因だった**(足したら 1.69 度縮んだ)
- 言える: 位相を足した模型は、相手と**同じ水準**に並んだ
- **言えない**: 相手を上回った。差が雑音に埋もれている
- **注意**: 幅が 1.23 → 3.38 と広がった。位相ありの模型は**不安定になっている**

#### 選定規則が試験と逆順に並んだ(L7 の副産物)

学習率は**内側検証だけ**で選ぶと事前に決めてある。ところが位相ありの系では:

| lr | 内側検証 | 試験 |
|---|---|---|
| 0.003 | 9.00 | **11.10** |
| 0.01 | 8.24 | 11.99 |
| **0.03(採用)** | **7.69** | 12.47 |

**内側検証が良い候補ほど試験が悪い。** 規則どおり内側検証が最小の 0.03 を採ったが、
それは試験側で最悪の候補だった。位相なしの系では掃引がほぼ平ら(11.98〜12.08)
だったので、この問題は表に出ていなかった。

原因は、内側検証を**止め時の選択にも使っている**ことである。学習率が高いほど内側検証の
谷を速く深く掘れるが、そこで止めた重みは外へ出ない。つまり内側検証の値は
選択に使った時点で偏った推定量になっており、**学習率という別の軸の比較には使えない**。

**規則は事前に決めたので動かさない**(動かせば試験を見て選んだことになる)。
記録するのは規則の限界のほうである。正しく直すには、止め時に使う分割と学習率を選ぶ分割を
分ける(nested にする)必要がある —— **L8 の課題**。

なお採用した 0.03 は最良点の中央値が 29 エポックで、幅が広がったのもこの学習率の高さで
説明がつく。**P-07 が「数の上では成立」にとどまったのは、選定規則のせいでもある。**

#### CH4 の切り分け —— §7.11 の宿題に答えた

L2 で G-10 は通ったが、下がったのが「CH4 が緯度の情報を足した」からか
「第 2 チャンネルが学習を安定させた」からか分けられていなかった。

分けるために、**CH4 チャンネルを地点年ごとに入れ替えた対照**を置いた。
入れ替えた CH4 は分布も滑らかさも本物のままで、**その地点年のものではない**だけである。
全 739 例で入れ替わっていることは実測で確かめてある(T-059)。

| 系 | MAE(地点) | 種の幅 | 基準との差 |
|---|---|---|---|
| CO2 のみ(基準) | 11.69 | 2.63 | — |
| CO2 + **本物の** CH4 | 10.76 | 0.40 | **−0.93** |
| CO2 + **入れ替えた** CH4 | 14.17 | 2.03 | **+2.48** |

**「チャンネルが増えたから安定した」という説明は落ちた。** その説明が正しければ、
入れ替えた CH4 も同じだけ助けるはずである(同じ分布・同じ滑らかさ・同じチャンネル数)。
実際には **2.48 度悪化した**。

したがって **CH4 が効いたのは情報である。** 種の幅が 2.63 → 0.40 と縮んだのも、
情報の結果であって別の原因ではない —— 入れ替えた側の幅は 2.03 で縮んでいない。

- 言える: **CH4 の年周曲線は、その地点年の緯度についての手がかりを実際に持っている**
- 言える: 手がかりが揃っていると学習も安定する(幅 2.63 → 0.40)
- **言えない**: CH4 の何が緯度を指しているのか。位相か、振幅か、CO2 との位相差か。
  それを分けるには CH4 側の要素を個別に潰した対照が要る(**L8 の課題**)

§7.11 に書いた留保はこれで解けた。**P-03(CH4 は効かない)は外れたままで、
外れ方の中身が分かった。**

### 7.17 学習率の選定を nested にし、CH4 の要素を切り分ける(L8 — **測る前に書いた** 2026-09-14)

**この節の判定の線は、測定を走らせる前に書いた。** 結果は §7.18 に書き、この節は書き換えない。

#### 選定の組み方

外側で抜いた群を除いた群を、**止め時を選ぶ分割(20%)**と**学習率を選ぶ分割(20%)**に
排他に分け、残り 60% で学ぶ。学習率の三候補(0.003 / 0.01 / 0.03)をそれぞれ学習し、
止め時は止め時の分割で選ぶ。**学習率は fold ごとに、選ぶ分割の誤差が最小の候補を採る。**
三候補ぶんの試験予測は報告に残すが、選ぶのには使わない。

同じ重みの上で、**L7 の規則(止め時の分割で学習率も選ぶ)を当てた場合**の試験誤差も出す。
重みが同じなので、二つの差は**選定規則だけの差**になる。

訓練に回る群は 75% から 60% に減る。相手の 2 特徴 kNN は外側の訓練群を全部使うので、
**この比較は模型に不利な向きにずれる** —— 結論を甘くする向きではない。

#### 事前登録した判定の線

| ID | 判定の線 |
|---|---|
| P-07(再判定) | nested の位相ありの MAE が 10.95 を下回れば「数の上では成立」。**「上回った」と言うのは、差が位相ありの種の幅を超えたときだけ** |
| P-08 | nested の位相ありの種の幅が 3.38 を下回る |
| P-09 | 同じ重みの上で、選ぶ分割で選んだ試験 MAE(3 種平均)が、止め時の分割で選んだ試験 MAE を下回る。位相ありで判定する |
| P-10 | CH4 の効き目の残り具合を r = (基準 − 対照) / (基準 − 本物) で測る(基準 = CO2 のみ、本物 = 本物の CH4)。**振幅を揃えた対照で r < 0.5、巡回でずらした対照で r ≥ 0.5 なら成立。** ただし分母の 0.93 度は基準の種の幅 2.63 より小さく、r は種で大きく揺れる。**要素を「特定した」と言うのは、3 種すべてで対照が同じ側に落ちたときだけ**(種ごとに、対照の MAE が本物と基準のどちらに近いか)。揃わなければ数の上での判定にとどめる |
| P-11 | nested の位相ありの MAE が、nested の位相なしの MAE を下回る |

#### CH4 の要素を潰す対照

- **巡回でずらした CH4**: 地点年ごとに 6〜18 ビン(四半期〜四分の三年)巡回させる。
  振幅と形は本物のまま、**CO2 との位相差だけ**が壊れる。位相なしの模型は二つのチャンネルを
  揃って回すことには不変だが、片方だけ回すと差が見える
- **振幅を揃えた CH4**: 地点年ごとに標準偏差で割り、全例の標準偏差の中央値を掛ける。
  形と CO2 との位相差は本物のまま、**振幅の大小だけ**が消える
- 土俵は L7 の CH4 表と同じ(739 地点年・位相なし・lr=0.03)。**再現の検算**として、
  本物の CH4 の種 0 を測り直して L7 の 10.91 と一致することを先に確かめる
  —— 学習率の既定値(0.01)のまま走らせる事故を、数で捕まえるため

### 7.18 nested で測り直した結果と、CH4 の要素(L8 — 実測 2026-09-15)

§7.17 に測る前に書いた線で判定する。線は動かしていない。

#### 再現の検算

本物の CH4 の種 0 を同じ土俵で測り直すと **10.9114** 度で、L7 の値と差 0.0 で一致した。
学習率・要約・母集団が L7 の CH4 表と同じであることを、数で確かめてから対照を走らせた。

#### 学習率の nested 選定

| 系 | nested で選んだ MAE | 種ごと | 幅 | 止め時の分割で選んだ MAE |
|---|---|---|---|---|
| **1D CNN(位相あり・nested)** | 11.55 | 12.14 / 11.62 / 10.88 | 1.26 | 11.88 |
| 1D CNN(位相なし・nested) | 12.52 | 12.87 / 13.45 / 11.24 | 2.21 | 12.59 |
| 相手(2 特徴 kNN) | 10.95 | — | — | — |

- **P-07(再判定)— 不成立。** nested で選ぶと位相ありは 11.55 度で、相手の 10.95 度を下回らない。
  L7 の 10.71 度は、偏った選定規則(§7.16)と種の幅 3.38 の上で出た数だった。
  訓練に回る群が 75% → 60% に減っているので、この比較は模型に不利な向きにずれている ——
  **それでも、L7 で「相手と並んだ」を支える数は残らなかった**
- **P-08 — 成立。** 位相ありの種の幅が 3.38 → **1.26** に縮んだ。
  fold ごとに選ばれた学習率は 3 種とも三候補に散っており、全 fold が高い学習率に乗ることがなくなった
- **P-09 — 成立(平均で)。** 同じ重みの上で、選ぶ分割で選ぶと 11.55 度、止め時の分割で選ぶと 11.88 度。
  ただし**種 2 だけは逆**(10.88 対 10.22)で、3 種すべてで良くなったわけではない。
  位相なしでも平均では 12.52 対 12.59 と同じ向きだが、差は 0.07 度しかない
- **P-11 — 成立。** nested でも位相あり 11.55 度 対 位相なし 12.52 度で、位相ありが 0.97 度良い。
  ただし **L7 の 1.69 度から縮んでおり、差 0.97 は位相なしの種の幅 2.21 より小さい。**
  言えるのは「位相の効果は選定規則を替えても向きが保たれた」までで、大きさは確かでない

#### nested にして見えた副産物 —— 止め時の側が L7 と別の振る舞いをした

止め時の分割を 25% から 20% に縮めて選定の分割と分けると、学習率 0.003 の最良点の中央値が
位相ありの種 0 で 31、種 1 で 22 エポックになった(L7 の掃引では同じ学習率で 377)。
種 2 では 559 エポックで、種による違いが大きい。位相なしでも種 0/1 で 33/39、種 2 で 549 と同じ形である。
**機構は測っていない。** 考えられるのは「約 11 群しかない止め時の分割は検証誤差の揺れが大きく、
学習の序盤の偶然の谷で止まる」ことだが、これは仮説であって確かめていない。

#### CH4 の要素

| 系 | MAE(地点) | 種の幅 | 残り r |
|---|---|---|---|
| CO2 のみ(基準・L7 再掲) | 11.69 | 2.63 | 0 |
| CO2 + 本物の CH4(L7 再掲) | 10.76 | 0.40 | 1 |
| CO2 + **巡回でずらした** CH4 | 11.62 | 0.30 | 0.08 |
| CO2 + **振幅を揃えた** CH4 | 11.23 | 0.29 | 0.50 |

r = (基準 − 対照) / (基準 − 本物)。1 なら本物の CH4 と同じだけ効き、0 なら CH4 が無いのと同じ。

- **P-10 — 外れた。** 予測は「振幅を揃えると効き目が消え、位相差を壊しても残る」だった。
  実際は逆向きで、**振幅を揃えても効き目の半分(r = 0.50)が残り、
  CO2 との位相差を壊すと効き目がほぼ消えた(r = 0.08)**
- ただし**要素を特定したとは言わない。** 振幅を揃えた対照は 3 種とも本物寄りに落ちたが、
  巡回でずらした対照は 3 種中 2 種で本物寄りに落ちており、平均の r と食い違う。
  分母の 0.93 度が基準の種の幅 2.63 より小さいので、種ごとの側は揺れる
- 言える: **CH4 の効き目は振幅だけでは説明できない。** 振幅の大小を消しても半分が残る
- 言えない: 効き目を運んでいるのが CO2 との位相差である、とまでは言えない(種で揃っていない)

P-03(CH4 は効かない)は L2 で外れ、P-10(効くなら振幅)も外れた。
**CH4 について事前に書いた予測は、二つとも外れている。**

#### 所要時間の記録について(N-04)

一単位(全 fold の 1 エポック)は 739 ms、nested 1 系あたりの見積もりは 1.1 時間だった。
**位相なしの種 1 の所要 25,864 秒は学習時間ではない** —— 途中で機械が約 7 時間スリープしており、
経過時間で測っているのでその分が混ざっている。成績の数値には影響しない(計算は復帰後にそのまま続いた)。
他の種は 1,638〜2,313 秒で、同じ機械で並行セッションの学習と CPU を分け合った値である。

## 8. 言えないこと

- 本プロジェクトは **緯度と年周曲線の対応**を測る。**因果を測らない。**
  「北で振幅が大きいのは陸上植生が多いから」は文献の説明であって、本データからは出ない。
  画面には対応までを書き、機構の断定を書かない
- 気温・海氷・エルニーニョ指数などの外部指標は**入力に含めない**。
  相関の提示は因果の主張へ滑りやすく、絵にも精度にも寄与しないため(§10)
- 振幅の経年変化(P-06)は、観測網の変更・測器の更新・欠測の偏りと交絡しうる。
  交絡を測れる範囲で測り、測れない分は「測れていない」と書く

## 9. ループ計画(見込み — HC-077)

| ループ | 内容 |
|---|---|
| L0 | 足場・SRC-CO2/CH4 取り込み・地点メタと緯度の確定・船舶帯の検算(G-05)・census |
| L1 | 年周曲線の構成(event → 半月 24 点)・5 系のベースライン・陰性対照・P-01 閾値確定・G-12 判定 **完了** |
| L2 | **JAX(素・Flax なし)**で 1D CNN を学習。地点群ばらし・G-08/G-09/G-10 判定 **完了** |
| L3 | 重み書き出し + TS 前向き実装 + 二実装照合(G-01) **完了** |
| L4 | 画面①「呼吸する地球」(F-01/F-02)・Next.js 静的書き出しの足場・実ブラウザ検品 **完了** |
| L5 | 画面②「積み上がる息」(F-03)・気象庁取り込みと外部検証(F-08 / G-11) **完了** |
| L6 | 画面③「当ててみる」(F-04/F-05)・**位相不変性の発見**(§7.15) **完了** |
| L7 | **位相を見える模型で測り直す(P-07 判定)**・CH4 の切り分け対照(§7.11)・画面④「解剖台」(F-06/F-07/F-09) **完了** |
| L8 | **学習率の選定を nested にする**(§7.17/§7.18・P-07 は nested で不成立)・CH4 の何が効いたかの切り分け(P-10 は外れた)・解説(歩き方・設計図)・フッタ・初回デプロイ **完了** |

**8 ループ見込みである。** 実績は L0〜L8 の 9 ループで、見込みの L8 のとおりに閉じた(L0 を数えるかどうかの違いで、計画の中身は増えていない)。 確かめる手段: 各ループの `loop_end` に実績を記録し、
3 ループごとに残り見込みを引き直してこの節を更新する。当初の見込みは消さない。

## 10. スコープ外

- 将来の CO2 濃度の予測(トレンドの外挿は本プロジェクトの主題ではない)
- 気温・海氷・ENSO・人為排出量など外部指標との関連づけ(§8)
- 衛星観測(GOSAT / OCO-2)の取り込み
- ブラウザ内での学習(学習は手元、ブラウザは推論のみ)
- 経度・標高・地点名を入力に使うこと(緯度の手がかりが漏れる — G-02)
