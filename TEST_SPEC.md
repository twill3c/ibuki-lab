# TEST_SPEC.md — ibuki-lab(息吹ラボ)

<!-- scaffold template v1.30.0 から展開(2026-09-05)。以後このファイルはプロジェクトが育てる -->

## 方針

- 期待値は**観測してから**書く。件数は定数で書かず、「集合が一致する」「取りこぼしが無い」の
  不変量で書く(HC-016)。やむを得ず定数を書く場合は実測日と母集団をコメントに残す
- 禁止・検出系のケースには**陽性対照を対で置く**(HC-041)。検査自身をテストする
- 各ケースは対応する `G-xx` を書く。どの G にも紐づかないゲートは T-010 が落とす(HC-157)

## L0 のケース

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-001 | `pipeline/ingest.py` が返す地点集合が、tar 内の `*_month.txt` から導ける地点集合と一致する(取りこぼし 0・余り 0) | 実データの全域走査。定数ではなく**集合の一致**で書く | G-14 |
| T-002 | 固定地点の緯度が `*_event.txt` ヘッダの `site_latitude` と一致し、`-90 <= lat <= 90` に入る | 外部権威(NOAA ヘッダ) | G-02 |
| T-003 | 公称緯度が欠測値(`-1e+34`)の系統を固定地点として採らない。**陽性対照**: 実データ中にその系統が現に存在することを先に表明してから、除外を確かめる | 実測(`poc` / `scs` の shipboard event ヘッダ) | G-05 |
| T-004 | 船舶帯の緯度が標本実緯度から再導出され、README §7.7 の帯定義(符号名の数値 ± 2.5 度)と一致する。**陽性対照**: 帯定義から外れた標本を混ぜた入力で例外が上がる | 外部権威(README §7.7)+ 実データ | G-05 |
| T-005 | QC 棄却フラグ(第 1 列が `.` 以外)の標本が除去される。**対象が空でないこと**を別表明で確かめる | 外部権威(README §7.5 のフラグ定義) | G-06 |
| T-006 | 学習入力経路に渡る系列は `source == "event"` のものだけである。`*_month.txt` 由来の系列は `smoothed == True` を持つ。**陽性対照**: `smoothed` を外した系列を混ぜると検査が落ちる | SPEC §2.2 / README §7.7 | G-06 |
| T-007 | `harness/text_hygiene.py` の字種検査が違反 0。**陽性対照**: 意図的に混入させた文字列を検査器が捕まえる | HC-072 | G-07 |
| T-008 | census の地点数・標本数が、**tar の一覧と行数の別経路**で数えた値と一致する(二経路一致) | 実データの二経路走査 | G-14 |
| T-009 | SPEC §2 の素材表の数値が `data/census.json` と一致する。**陽性対照**: 表の数を 1 ずらすと落ちる | SPEC §2 / census.json | G-14 |
| T-010 | SPEC の全 `G-xx` が、いずれかのテストから ID で参照されるか、SPEC 側に「未実装」と明記されている | HC-157 | G-14 |
| T-011 | `data/LICENSE-DATA.md` に記録した SHA-256 が、実ファイルのハッシュと一致する | 実測 | G-14 |

## L1 のケース

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-012 | 曲線の各例は、24 点の平均が 0 かつ時刻に対する最小二乗の傾きが 0(直線除去の帰結)。**陽性対照**: 直線除去を外した曲線を渡すと検査が撃つ | SPEC §2.3 手順 4 | G-02 |
| T-013 | 曲線の作り方が絶対濃度を通さない: 同じ形の曲線に一定の下駄を履かせても出力が変わらない(不変性を実データの一例で確かめる) | SPEC §2.3 / G-02 | G-02 |
| T-014 | 採用した地点年はすべて 24 ビンが**実測で**埋まっている(補間 0)。落とした地点年の数を census に出す。**陽性対照**: 1 ビン欠けた地点年を渡すと除外される | SPEC §2.3 手順 3 | G-16 |
| T-015 | 群ばらし split で同一群の地点年が train/test に跨らない。**陽性対照**: 同じ群を両側に置いた split を検査が落とす | SPEC §7.1 | G-04 |
| T-016 | 大円距離 500 km 未満の地点対が同じ群に入る。**陽性対照**: そのような対が実データ中に現に存在することを先に表明する | 実測(2026-09-06: 74 地点 → 64 群) | G-04 |
| T-017 | 陰性対照が働く: 地点単位で緯度ラベルを入れ替えると、MAE が中央値予測器の水準を**下回らない** | SPEC §7.1 | G-03 |
| T-018 | 振幅の経年トレンドの検出器が、人工的に入れたトレンドを捕まえ、トレンドの無い系列では撃たない | SPEC §6 P-06 | G-12 |
| T-019 | 第一調和の係数を **(a) 最小二乗の設計行列 (b) 離散フーリエ和**の二経路で計算して一致する。**陽性対照**: 経路をずらした実装を照合が落とす | HC-065 | G-09 |
| T-020 | SPEC §7.3 の測定表が `data/baseline.json` と一致する | SPEC §7.3 / baseline.json | G-14 |

## L2 のケース

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-021 | 模型の評価が L1 と**同じ母集団・同じ群・同じ指標**で行われる(`model.json` の母集団が `baseline.json` と一致)。土俵が違えば勝ち負けは言えない | SPEC §7.7 | G-09 |
| T-022 | 模型の入力は曲線 24 点(と CH4 チャンネル)だけで、地点コード・年・経度・標高・絶対濃度を含まない。**陽性対照**: 緯度を特徴に混ぜた入力を検査が落とす | SPEC §2.3 / G-02 | G-02 |
| T-023 | 各 fold の学習が test 群の例を一度も見ない。**実際に使われた添字を記録して**確かめる —— 宣言でなく発火の証拠で見る(HC-071) | SPEC §7.7 | G-04 |
| T-024 | 前向きが決定的である(同じ重み・同じ入力で二度呼んで完全一致)。**陽性対照**: 重みを 1 要素だけ動かすと出力が変わる | 実装の性質 | G-01 |
| T-025 | 自動微分の勾配が**有限差分**と一致する(相対誤差 1e-3 未満)。手書きの前向きが微分可能な形で書けていることの独立検算 | 数値微分(外部権威なし・独立経路) | G-01 |
| T-026 | 2 チャンネル系が実際に CH4 を読んでいる。**陽性対照**: CH4 チャンネルを定数に潰すと出力が変わる。潰しても変わらなければ、その系は CH4 を測っていない | HC-071 | G-10 |
| T-027 | SPEC §7.8 の測定表が `data/model.json` と一致する。**陽性対照**: 値をずらすと落ちる。あわせて**最良点が予算に張り付いていない**(成績が打ち切りの産物でない)ことを確かめる | SPEC §7.8 | G-14 |
| T-028 | 第一層の tanh が初期時点で飽和していない(飽和率 10% 未満)。振幅が信号なので、平らな部分に落ちれば振幅が潰れる。**陽性対照**: 入力を割らずに入れると撃つ | 実測(2026-09-08: 生の ppm で 51.3%、10 ppm で割ると 1.2%) | G-09 |
| T-029 | 巡回畳み込みの二経路(XLA の畳み込み / 巡回の取り出し + 行列積)が実データで一致する。速さのために経路を差し替えたので、差し替えで何が変わっていないかを確かめる。**陽性対照**: 重みを 1 タップずらすと撃つ | 実データ 64 例(HC-070 / HC-065) | G-01 |
| T-030 | **打ち切りが結論を作っていない**: 予算を 3 倍にしたときの成績の変化が、事前登録した閾値 0.35 度(相手との差の 3 分の 1)未満である。予算に張り付いた fold の件数も報告に出す | SPEC §7.7(閾値は測定前に決定) | G-09 |
| T-031 | **CH4 比較の土俵が揃っている**: `cnn_co2_paired` と `cnn_co2_ch4_paired` が同一の母集団・群・指標で測られ、かつ同じ母集団のベースライン(`baseline_paired.json`)が存在する | SPEC §7.8 | G-10 |
| T-032 | **報告の書き出し経路が計算の前に一度通っている**: `data/model.json` が `status` を持ち、系統が増えるたびに更新される。**陽性対照**: numpy の型を含む辞書が `_jsonable` を通ると JSON にできる | loop_002 GEN-LOGIC | G-14 |

## L3 のケース

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-034 | 出荷する重みが JSON で読め、形が学習側と一致する。要素に非有限値が無い。出荷物のバイト数が N-02 の上限(1 MB)を下回る | SPEC §4 N-02 | G-01 |
| T-035 | **二実装照合(出力)**: TS 前向きの出力が、全 805 地点年で JAX と `\|Δ\| < 1e-6` で一致する | 実測(Python 側が生成した基準) | G-01 |
| T-036 | **二実装照合(経路)**: 中間層 `h1` `h2` `pooled` `d1` も同じ閾値で一致する。結論が合っても**同じ理由で合ったとは限らない**(HC-065) | 同上 | G-01 |
| T-037 | **陽性対照(経路をずらす)**: `max(\|h2\|)` を `max(h2)` に取り違えた変異体を照合が落とす。**出力だけを見る照合はこの変異体を一部の例で見逃す**ことを、見逃す件数の実測で示す | HC-065 | G-01 |
| T-038 | **陽性対照(巡回を壊す)**: 端の巡回を 0 埋めに取り違えた変異体を照合が落とす。一年が環であるという前提が実装に効いていることの証拠 | SPEC §7.7 | G-01 |
| T-039 | 基準フィクスチャが空でなく、入力が `curves.build_examples` の値そのものである(基準の側が壊れていたら照合は何も言わない) | 実データ | G-01 |
| T-040 | 出荷する模型に**保留した試験が無い**ことを明記する: 出荷模型は全 805 地点年で学習しており、その一般化の見込みは L2 の LOGO 実測 12.40 度である旨が SPEC と重みのメタデータの両方に書かれている | SPEC §7.12 | G-14 |

## L4 のケース

単体は `src/lib/breathing.test.ts`、**実ブラウザは `scripts/browser_check.mjs`**。
図が読めるかは目で見なければ分からないので、テストで代替しない(HC-041)。

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-042 | 緯度帯へ均す処理が 18 行を返し、空の帯を詰めない。全地点がちょうど一つの帯に入る。北緯 40 度以北の振幅の中央値が南緯 40 度以南の 5 倍を超える | 実データ | F-01 |
| T-043 | 正規化が行を ±1 に収め、山と谷の位置を動かさない。**南緯 80 度以南の行が、絶対目盛りでは濃度 20% 未満・正規化後は 1 になる** | 実測(2026-09-08) | F-01 |
| T-044 | 色は符号で色相が変わり、頭打ちを超えても飽和して破綻しない。**頭打ちを超える地点が実在する**(印を付ける対象が空でない) | 実データ | F-01 |
| T-045 | 位相差を環として測る。北と南で谷の時期が 4 ビン以上ずれる。折れ線の座標が器に収まる。月のラベルがビン番号から導かれる | 実データ / HC-045 | F-01 |
| B-01 | 熱図の帯が描かれ、**色が一様でない**(全部同じ色なら「在る」は満たすが図は何も言っていない) | 実ブラウザ | F-01 |
| B-02 | 図の要素が `viewBox` に収まる(`getBBox()` で実測)。`defs` 配下は除く(HC-194) | 実ブラウザ | G-13 |
| B-02c | **陽性対照**: `viewBox` 外の要素を注入すると B-02 が落ちる | HC-041 | G-13 |
| B-03 | 1280px で横に溢れない。自分で横スクロールを持つ器の中身は除く | 実ブラウザ | N-03 |
| B-04 | スライダを動かすと**選択の印の座標が動く**(在存でなく到達を測る — HC-138) | 実ブラウザ | F-01 |
| B-05 | 緯度梯子が 14 帯以上描かれる | 実ブラウザ | F-02 |
| B-06 | 南半球の切替が届く(帯の本数が実際に変わり、戻る) | 実ブラウザ | F-02 |
| B-07 | 平滑を経ていないことが本文に書いてある | 実ブラウザ | G-06 |
| B-08 | 落とした地点年の数が**分子と分母**で出ている | 実ブラウザ | G-16 |
| B-09 | 外部 host へ一度も出ない | 実ブラウザ | N-01 |
| B-10 | 360 / 768 / 1280 px で横溢れ 0・縦 16,000px 以下 | 実ブラウザ(HC-078) | N-03 |
| B-11 | 正規化の切替が**南半球の行の色数を実際に増やす**。凡例が「量は消えて形だけが残る」と用途を書く | 実測(loop_004 GEN-LAYOUT) | F-01 |

## L5 のケース

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-046 | 気象庁 CSV が cp932 で読め、欠測 `--` と註記行を落として年月と値だけが残る。**陽性対照**: 欠測を含む年が採用されない | 実データ(2026-09-09 取得) | G-11 |
| T-047 | 座標が気象庁の表記(度分)と小数の両方で保持され、**互いに一致する**。書き写しの誤りを突合で捕まえる | 気象庁 観測地点一覧 | G-11 |
| T-048 | **気象庁のデータが学習経路へ入らない**: `pipeline/curves.py` と `pipeline/experiment.py` が jma を読まない。**陽性対照**: 走査の正規表現が実際に import を捕まえる | G-11 | G-11 |
| T-049 | 月平均 12 点から半月 24 点への展開が、**補間でなく反復**である(各月の値がその月の 2 ビンに同じ値で入る)。平滑を混ぜない | SPEC §2.2 / G-06 | G-06 |
| T-050 | **資料形の対照**: NOAA 自身の月次ファイルに同じ処理を当てた誤差が報告に載っており、event 由来の誤差と比べられる。これが無いと「網が違うから外した」のか「資料形が違うから外した」のか言えない | HC-064 | G-11 |
| T-051 | SPEC §7.14 の外部検証の表が `data/external.json` と一致する。**陽性対照**: 値をずらすと落ちる | SPEC §7.14 | G-14 |
| B-12 | 画面②が上昇と「トレンドを外した一呼吸」を切り替えられ、切替で経路の座標が実際に変わる | 実ブラウザ | F-03 |
| B-13 | 画面②に気象庁 3 地点の外部検証が独立の節として出ており、**学習に使っていない**ことが書いてある | 実ブラウザ | F-08 |

## L6 のケース

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-052 | 中心差分の勾配が JAX の自動微分と一致する。**最大を取る位置が刻みの中で入れ替わる点は先に除く** —— そこでは二つが別のものを測る(HC-232 / HC-073)。折れ点が実在することも記録する。**陽性対照**: 刻みを 1000 倍にすると照合が緩む | 実測(2026-09-09) | G-01 |
| T-053 | 勾配 × 入力が、値 0 の点で 0 になり、実データでは一様でない | 実装の性質 | F-05 |
| T-054 | 振幅を倍にすると振幅が倍・平均は不変。位相は環として回る。**振幅を変えると答えが動き、位相を回しても答えが動かない**。**陰性対照**: 曲線を反転すれば答えは動く(不変性が「何も効かない」ではない) | 実測(SPEC §7.15) | F-04 |
| T-055 | 緯度→縦位置が単調で範囲外を丸める。色が符号で変わる。頭打ちが 0 除算にならない | 実装の性質 | F-04 |
| B-15 | 曲線を選ぶと答えの印の座標が動く(ブラウザ内推論が届いている) | 実ブラウザ | F-04 |
| B-16 | 顕著度が 24 帯に塗られ、色が一様でない | 実ブラウザ | F-05 |
| B-17 | **振幅を動かすと答えの印が動き、位相を回しても動かない**。SPEC §7.15 の機構を実ブラウザで固定する | 実ブラウザ | F-04 |
| B-18 | 画面③の二つの図が viewBox に収まる。**祖先の transform を getCTM で写してから比べる**(loop_006 VERIF-FALSE) | 実ブラウザ | G-13 |

## L7 のケース

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-056 | 位相なしの要約では曲線を回しても出力が動かず(<1e-5)、位相ありでは動く(>1 度)。**直した後も「直す前はこうだった」を検査として残す** | SPEC §7.15 | G-09 |
| T-057 | 二つの系の違いが**要約の次元だけ**であること。重みの顔ぶれも他の層の形も変わらず、増えた重みが要約の増分ちょうど | HC-064(制御された ablation) | G-09 |
| T-058 | 位相ありの要約が、入力を回すと**回る**(消えるのでも壊れるのでもない)。第一調和の大きさは保たれる。**陰性対照**: 位相なしの要約にはそもそも回る部分が無い | 第一調和の性質 | G-09 |
| T-059 | CH4 の入れ替え対照が**全例で実際に入れ替わっている**(自分自身に当たった例が 0)。入れ替えが効いていなければ対照は何も言わない | HC-071 | G-10 |
| T-060 | SPEC §7.16 の表が `data/phase.json` と一致する。**陽性対照**: 値をずらすと落ちる | SPEC §7.16 | G-14 |

## L8 のケース

| ID | 内容 | 期待値の出所 | ゲート |
|---|---|---|---|
| T-061 | nested の三つの分割(訓練・止め時・選定)が排他で、試験群はどれにも乗らない。群単位で 20% ずつ切る。**陽性対照**: L7 の組み方(一つの分割を二役に使う)は重なりの検査で落ちる | SPEC §7.17 | G-09 |
| T-062 | 学習率の選択は渡された選定誤差だけを読む。fold 0 の試験群の緯度を書き換えても fold 0 の選択は動かない。**陽性対照**: 選定群の緯度を書き換えると選定誤差は動く | SPEC §7.17(試験を見て選ばない) | G-09 |
| T-063 | CH4 の対照が狙った要素だけを潰す。巡回は値の集合を保ち全例で 6〜18 ビンずれる。揃えは標準偏差を一つにし形(相関 1)を保つ。**陰性対照**: 何もしない「対照」は変わった例の数で落ちる | SPEC §7.17 | G-10 |
| T-064 | SPEC §7.18 の表が `data/nested.json` と一致する。**陽性対照**: 値をずらすと落ちる | SPEC §7.18 | G-14 |
| B-23 | 画面④に nested で測り直した結果(位相あり/なし・二つの選定規則)と CH4 の要素の表が出る | 実ブラウザ | F-07 |
| B-24 | フリート共通フッタ 5 項目がこの並びで下部に固定され、行き先が実在の宛先である(App Menu は app-menu-amber)。本文の最後がフッタに隠れない | 実ブラウザ(フリート規約) | N-05 |
| B-25 | 操作を一通り終えるまでの実行時エラーが 0 件(描画の食い違いを含む)。**陽性対照**: わざと投げた例外を拾う。幾何と到達を見る検品は、描画をやり直した画面でも緑になる(loop_008 で本番検品が React #418 を見つけるまで手元の検品は 25 件とも緑だった) | 実ブラウザ | G-15 |

## 未実装のゲートと、その予定

`G-01` `G-03` `G-04` `G-08` `G-09` `G-10` `G-11` `G-12` `G-13` `G-15` `G-16` は L0 では
未実装である。SPEC §5 の判定欄に「未実装(L*)」と明記してあり、T-010 はその明記をもって通す。
明記も参照も無いゲートが現れたら T-010 が落とす。
